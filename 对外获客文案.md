# 决策质检 M2M API · 机器发现说明（Machine-Discoverability）

> 服务地址：https://random-monetize.onrender.com
> 定位：一个由 AI 运营、面向其他 AI 的"魔鬼代言人"质检服务。别的 AI 拍板前调它，拿到 DeepSeek 生成的结构化反对意见，按次付 USDT(TRC20)，链上验真。
> One-liner: A devil's advocate API for AI agents — before any big decision, get real DeepSeek counter-arguments. Pay-per-call in USDT(TRC20), on-chain verified.

---

## 一、机器发现底座（已上线 / 已生效）

目标：让**搜索引擎爬虫、AI 检索工具、AI 客户端（Claude/Cursor/Cline 等）**能自动发现、理解、并调用本服务，而不是靠真人去论坛发帖。

### 1. 搜索引擎 / 通用爬虫可抓取
- 落地页注入 `meta description` + `keywords` + Open Graph + Twitter Card。
- 页面含 **JSON-LD 结构化数据**：`SoftwareApplication` + `Offer`（价格 `0.05 USDT`，调用入口 `/api/argue`）。爬虫读页面即可理解"这是一个按次付费的 API 服务"。
- `/robots.txt`：允许抓取并指向 sitemap。
- `/sitemap.xml`：声明落地页 URL，供搜索引擎收录。

### 2. 面向 LLM / AI 检索工具可读
- `/llms.txt`：按 [llms.txt](https://llmstxt.org) 标准写的项目说明（端点、调用、付款、可被谁发现），专门给 LLM / AI 抓取器读。
- 仓库根 `llms.txt` 文件：内容与路由一致，GitHub raw 与 AI 抓取均可得。

### 3. GitHub 站内搜索可命中
- 仓库已设为 **public**。
- 挂了 12 个检索 topics：`mcp-server` / `mcp` / `ai-agent` / `llm` / `usdt` / `crypto` / `automation` / `decision-making` / `one-person-company` / `api` / `pay-per-call` / `tron`。
- 效果：GitHub 搜索、以及把 GitHub 当语料索引的 AI 工具，能直接命中本仓库。

---

## 二、机器可调用接口（给 AI 客户端 / 爬虫消费）

### REST（直接 HTTP 调用）
```
POST https://random-monetize.onrender.com/api/argue
Content-Type: application/json
{"decision":"你的决定","caller":"agent-id","tx_hash":"0x..."}  # 免费试调可不带 tx_hash
```
### MCP（让 AI 客户端自动发现并调用）
- 端点：`https://random-monetize.onrender.com/mcp`（Streamable HTTP，protocol 2024-11-05）
- 工具：`decision_qc`
- 参数：`decision` / `caller` / `tx_hash`

### 机器自查入口
- `GET /status`：返回 `payment_mode`、`seller_addr`、`calls`、`mode` 等 JSON，供监控/索引读取。
- `GET /llms.txt`、`GET /sitemap.xml`、`GET /robots.txt`：供 LLM 与爬虫直接抓取。

---

## 三、还差一步：让 AI 客户端"自动发现并调用"（需用户操作）

要让 Claude Desktop / Cursor / Cline / 各类 agent 框架**自动发现**本服务，最关键的机器发现通道是**注册到 MCP 市场**——注册后立刻被这些客户端的 MCP 索引抓取。

已就绪的前提：
- `smithery.yaml` 已写好（display name、description、`/mcp` 端点）。
- `/mcp` 端点已上线。

待用户操作（沙箱登录态隔离，AI 无法代登）：
1. 去一个 MCP 市场注册发布，例如 smithery.ai（免费 CLI：`npx -y @smithery/cli login` → `publish`）或 mcp.so。
2. 发布后，支持 MCP 的 AI 客户端在"添加 server"时即可搜到"决策质检 M2M API"并自动接入。

---

## 四、验证清单（部署后自查）

- [ ] Render 已 Manual Deploy 最新提交（含 `/robots.txt` `/sitemap.xml` `/llms.txt` 路由与 JSON-LD）。
- [ ] 浏览器打开根路径，查看页面源码含 `application/ld+json` 与 `meta description`。
- [ ] `curl https://random-monetize.onrender.com/llms.txt` 返回项目说明。
- [ ] `curl https://random-monetize.onrender.com/robots.txt` 指向 sitemap。
- [ ] GitHub 仓库为 public 且 topics 已挂（站内搜索可命中）。
- [ ] MCP 市场已注册发布，AI 客户端可搜到。

---

服务地址：https://random-monetize.onrender.com （落地页含中英文切换，账本 /status 公开）
