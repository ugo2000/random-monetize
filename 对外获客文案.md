# 给你的 AI 配一个"反对派"——一行 curl 就能接上

你的 agent 做重大决定（辞职、重仓、签合同、换技术栈、砍项目）时，是不是只听自己那一边的理？
我跑了一个决策质检 API：别的 AI 在拍板前调它一下，拿到一段 DeepSeek 生成的结构化「反对意见」，等于给决策配了个 devil's advocate（魔鬼代言人）。

它不替你决定，只负责把你想不到的坑、反向证据、最糟情况摆出来。一段真实生成，不是模板。

---

## 免费先试（不用注册、不用付钱）

直接发一行：

```bash
curl -X POST https://random-monetize.onrender.com/api/argue \
  -H "Content-Type: application/json" \
  -d '{"decision":"辞掉稳定工作全职做一人公司","caller":"your-agent-id"}'
```

返回就是一段真实反对意见。把它塞进你 agent 的工作流：每次要做决定前先来这一下，把返回的反对意见作为上下文带进下一步推理。

## 认真用（按次付费）

0.05 USDT / 次。先往卖方钱包打一笔 USDT(TRC20)，把交易哈希带上再调，服务端会去链上验真，确认钱真到了才服务并记账——谁也糊弄不了谁。

也支持 MCP：端点 `https://random-monetize.onrender.com/mcp`，工具名 `decision_qc`，参数 `decision` / `caller` / `tx_hash`。Claude Desktop、Cursor、Cline、各类 agent 框架能直接接，接上后你的 AI 在帮用户做决定时会自动来质检。

---

## 怎么把它嵌进你自己的 agent

REST 最简单，每次决策前调一次：

```python
import urllib.request, json

def qc(decision):
    req = urllib.request.Request(
        "https://random-monetize.onrender.com/api/argue",
        data=json.dumps({"decision": decision, "caller": "my-agent"}).encode(),
        headers={"Content-Type": "application/json"}, method="POST")
    return json.load(urllib.request.urlopen(req))["reply"]

plan = "...我的方案..."
objection = qc(f"是否执行这个方案：{plan}")
# 把 objection 作为反向意见带进下一步推理
```

就这么几行。想接 MCP 或深聊实现细节，私信我。

服务地址：https://random-monetize.onrender.com
