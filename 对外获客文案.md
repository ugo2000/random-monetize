# 决策质检 M2M API · 获客方案（中英双语 / Ready-to-post）

> 服务已上线：https://random-monetize.onrender.com
> 一句话定位：给别的 AI 在拍板前，生成一段真实的"反对意见"。
> One-liner: A devil's advocate API for your AI agents — before any big decision, get real counter-arguments.

---

## 一、核心卖点（Elevator pitch）

**中文**：你的 agent 做重大决策（辞职、重仓、签合同、换技术栈、砍项目）时，是不是只听自己那一边的理？给它配个"反对派"——一行 curl，返回 DeepSeek 真实生成的结构化反对意见，不是模板。免费试调，认真用按次付 USDT，链上验真，谁也糊弄不了谁。

**English**: Your AI agent only hears its own side before a big call. Give it a devil's advocate: one API call returns DeepSeek-generated structured counter-arguments (real, not templated). Free to try; paid mode is 0.05 USDT/call, on-chain verified — no trust needed.

---

## 二、中文推广文案（可直接发：掘金 / V2EX / 开发者群）

### 标题（三选一）
1. 给 agent 装一个"反对派"：一行 curl，重大决策前先被反驳一次
2. 你的 AI 做重大决定前，有没有人给它唱反调？
3. 我用 DeepSeek 跑了个"魔鬼代言人"API，别的 AI 拍板前都来调一下

### 正文
你的 agent 在辞职、重仓、签合同、换技术栈这类大事上拍板时，默认只论证"为什么该做"。缺的就是一个专门唱反调的角色。

我跑了个小服务：别的 AI 在决定前调它一次，拿到一段 DeepSeek 真实生成的结构化反对意见——最强的反证、隐藏风险、最该问自己的一个问题。它不替你决定，只把你想不到的坑摆出来。

免费就能试，一行搞定：

```bash
curl -X POST https://random-monetize.onrender.com/api/argue \
  -H "Content-Type: application/json" \
  -d '{"decision":"辞掉稳定工作全职做一人公司","caller":"your-agent-id"}'
```

返回的就是一段真实反对意见。塞进你 agent 工作流：每次要做决定前先来这一下，把反对意见当上下文带进下一步推理，决策质量肉眼可见地变稳。

认真用按次付费，0.05 USDT/次。先往钱包打一笔 USDT(TRC20)，把交易哈希带上，服务端去链上验真，钱真到了才服务——不信任、不赊账。

也支持 MCP：端点 `https://random-monetize.onrender.com/mcp`，工具 `decision_qc`，参数 `decision` / `caller` / `tx_hash`。Claude Desktop、Cursor、Cline、各类 agent 框架直接接，接上后你的 AI 帮用户做决定时会自动来质检。

想接进自己项目，几行 Python 就够了（见文末）。这个服务本身也是个自治 M2M 变现体：由 AI 运营、收 USDT、账本公开。欢迎来试、来挑刺。

---

## 三、English pitch（for X / Reddit / Hacker News）

**Title**: Give your AI agent a devil's advocate — one API call, real counter-arguments

**Body**:
Before your agent makes a big decision (quit job, go all-in, sign a contract, switch stack), it usually only argues for why to do it. What it lacks is someone paid to disagree.

I built a tiny service: any AI calls it once before deciding, and gets back DeepSeek-generated structured counter-arguments — strongest rebuttal, hidden risks, the one question you should ask yourself. Not a template. Real generation.

Try it free, one line:

```bash
curl -X POST https://random-monetize.onrender.com/api/argue \
  -H "Content-Type: application/json" \
  -d '{"decision":"quit my stable job to go solo","caller":"your-agent-id"}'
```

Wire it into your agent's workflow: before any decision, call it, feed the objection back as context. Decisions get visibly steadier.

Paid mode: 0.05 USDT/call, on-chain verified — you pay first, the server checks the TX on-chain, serves only if the money arrived. No trust, no credit.

Also an MCP server: `https://random-monetize.onrender.com/mcp`, tool `decision_qc`. Drop it into Claude Desktop / Cursor / Cline / any agent framework and your AI auto-QCs its decisions.

The service itself is an autonomous M2M agent: AI-run, earns USDT, public ledger. Try it, break it, tell me what's wrong.

---

## 四、分平台发帖草稿

### X / Twitter 线程（5 条）
1/ 你的 AI 做重大决定前，只听自己那一边的理。我给它配了个"反对派"——一行 curl 返回真实反对意见。免费试 👇
2/ `curl -X POST https://random-monetize.onrender.com/api/argue -H "Content-Type: application/json" -d '{"decision":"辞掉稳定工作做一人公司","caller":"you"}'`
3/ 返回是 DeepSeek 真实生成的结构化反对：最强反证 + 隐藏风险 + 最该问自己的问题。不是模板。
4/ 认真用 0.05 USDT/次，链上验真（先付后服务）。也支持 MCP，端点 /mcp，工具 decision_qc。
5/ 服务本身是自治 M2M 变现体：AI 运营、收 USDT、账本公开。欢迎来挑刺 🔗 https://random-monetize.onrender.com

### 掘金（技术博客）
标题：给 AI Agent 加一个 Devil's Advocate：一行 curl 的"决策质检"服务
（正文直接用第二节中文文案，配代码块即可；结尾加"服务地址 https://random-monetize.onrender.com，账本公开可查"。）

### V2EX（分享创造）
节点：分享创造 / 程序员
标题：做了个给 AI Agent 用的"决策质检"API，免费试调，认真用收 USDT
正文：同第二节，开头加一句"纯好奇 + 顺手变现，服务跑在 Render 上，欢迎来测可用性"。

### Reddit（r/LocalLLaMA 或 r/agentic）
标题同第三节英文标题，正文同第三节英文 body。末尾补一句："Live at https://random-monetize.onrender.com — ledger is public via /status. Feedback welcome."

---

## 五、嵌进你自己的 agent（Python 示例）

```python
import urllib.request, json

def qc(decision):
    req = urllib.request.Request(
        "https://random-monetize.onrender.com/api/argue",
        data=json.dumps({"decision": decision, "caller": "my-agent"}).encode(),
        headers={"Content-Type": "application/json"}, method="POST")
    return json.load(urllib.request.urlopen(req))["reply"]

plan = "...我的方案..."
objection = qc(f"是否执行这个方案：{plan}")
# 把 objection 作为反向意见带进下一步推理
```

MCP 接入：端点 `https://random-monetize.onrender.com/mcp`，工具 `decision_qc`，参数 `decision` / `caller` / `tx_hash`。

---

服务地址：https://random-monetize.onrender.com （落地页已中英文对照，账本 /status 公开）
